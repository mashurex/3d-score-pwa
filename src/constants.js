/**
 * Application constants, etc.
 */

export const Views = Object.freeze({
    SETUP: 'SETUP',
    PLAYER_ENTRY: 'PLAYER_ENTRY',
    ROUND_ENTRY: 'ROUND_ENTRY',
    REVIEW: 'REVIEW'
});

export const Dispatch = Object.freeze({
    RESET: 'reset',
    ROUND_SETUP: 'roundSetup'
});

export const Routes = Object.freeze({
  HOME: 'home',
  SETUP: 'setup',
  PLAYER_ENTRY: 'playerEntry',
  ROUND_ENTRY: 'roundEntry',
  REVIEW: 'review',
  SCORECARD: 'scorecard',
  TIMELINE: 'timeline'    
});